// Package openai implements a provider adapter for OpenAI-compatible chat
// completions APIs.
//
// It uses the standard net/http client and supports custom base URLs for
// local proxies or alternative endpoints. No external SDK is required.
package openai
